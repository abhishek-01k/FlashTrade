{
  "name": "metis-hardhat-template",
  "version": "1.1.0",
  "description": "Hardhat-based development environment for Metis",
  "main": "index.js",
  "scripts": {
    "build": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js",
    "node": "npx hardhat node",
    "lint": "echo 'Linting contracts...'",
    "verify": "npx hardhat verify"
  },
  "keywords": ["blockchain", "ethereum", "metis", "hardhat"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.7",
    "hardhat-deploy": "^1.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
